# fang

## Build Setup

```bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3000
$ npm run dev

# data refresh
$ cd cooker && node data.js
# Publish test
$ npm run generate 
$ ./docker.sh
# Publish
$ npm run generate
$ git push
```
